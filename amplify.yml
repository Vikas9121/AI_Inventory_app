version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - echo "REACT_APP_API_URL=$REACT_APP_API_URL" >> .env
        - echo "NODE_ENV=production" >> .env
        - echo "CI=false" >> .env
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*